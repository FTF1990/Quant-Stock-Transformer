# 更新说明 - Enhanced Gradio Residual Boost System

## 📋 本次更新内容

### ✅ 新增功能

#### 1. Tab 4: Stage2 Boost训练 🚀
**位置**: `Tab 4: 🚀 Stage2 Boost训练`

**功能**:
- 基于SST模型提取的残差训练Stage2残差模型
- 使用SST架构作为Stage2模型
- 完整的训练流程：数据准备 → 模型训练 → 性能评估 → 模型保存

**参数配置**:
```python
{
    'residual_data_selector': 选择残差数据,
    'd_model': 模型维度 (32-256, 默认128),
    'nhead': 注意力头数 (2-16, 默认8),
    'num_layers': Transformer层数 (1-8, 默认3),
    'dropout': Dropout率 (0-0.5, 默认0.1),
    'epochs': 训练轮数 (10-200, 默认100),
    'batch_size': 批大小 (16-128, 默认32),
    'lr': 学习率 (默认0.001),
    'weight_decay': 权重衰减 (默认1e-5),
    'grad_clip': 梯度裁剪 (0.1-5.0, 默认1.0),
    'test_size': 测试集比例 (0.1-0.3, 默认0.15),
    'val_size': 验证集比例 (0.1-0.3, 默认0.15)
}
```

**核心函数**:
```python
def train_stage2_boost_model(
    residual_data_key: str,
    config: Dict[str, Any],
    progress_callback=None
) -> Tuple[str, Dict[str, Any]]
```

**输出**:
- 训练日志（实时显示）
- 模型保存到 `saved_models/stage2_boost/`
- 包括模型权重(.pth)和Scalers(.pkl)

#### 2. Tab 5: 综合推理模型生成 🎯
**位置**: `Tab 5: 🎯 综合推理模型`

**功能**:
- 计算每个信号的R²分数
- 智能选择R²阈值（默认0.4）
- 只对R² < 阈值的信号应用Stage2修正
- 生成综合推理模型

**智能选择逻辑**:
```python
for each signal:
    if signal_r2 < threshold:
        # 应用Stage2修正
        final_prediction = sst_prediction + stage2_residual
    else:
        # 保持SST预测
        final_prediction = sst_prediction
```

**核心函数**:
```python
def compute_signal_r2_and_select_threshold(
    base_model_name: str,
    stage2_model_name: str,
    r2_threshold: float = 0.4
) -> Tuple[str, Dict[str, Any]]
```

**输出**:
- 每个信号的R²分析表格
- 性能对比（SST vs 综合模型）
- 改进百分比（MAE, RMSE, R²）
- 综合模型配置保存到 `saved_models/ensemble/`

#### 3. Tab 6: 二次推理比较 📊
**位置**: `Tab 6: 📊 二次推理比较`

**功能**:
- 选择任意index范围进行推理
- 对比综合模型 vs 纯SST模型
- 可视化性能提升
- 详细的性能指标分析

**参数**:
```python
{
    'ensemble_selector': 选择综合模型,
    'start_index': 起始Index,
    'end_index': 结束Index
}
```

**可视化内容**:
1. 预测对比曲线（True vs SST vs Ensemble）
2. 误差对比时序图
3. 误差分布直方图
4. 性能指标柱状图（MAE, RMSE, R²）

**核心函数**:
```python
def compare_reinference_ui(
    ensemble_name: str,
    start_idx: int,
    end_idx: int
) -> Tuple[str, plt.Figure]
```

#### 4. Tab 7: Sundial残差预测 🔮
**位置**: `Tab 7: 🔮 Sundial残差预测`

**功能**:
- 基于综合模型的最终残差
- 使用Sundial时序模型预测未来残差
- 以index作为时间参数

**参数配置**:
```python
{
    'ensemble_selector': 选择综合模型,
    'forecast_horizon': 预测步数 (10-500, 默认100),
    'd_model': 模型维度 (32-256, 默认64),
    'nhead': 注意力头数 (2-8, 默认4)
}
```

**状态**: ⚠️ 框架已搭建，具体实现待完善

### ❌ 删除内容

#### 1. Hybrid Transformer相关代码
**删除文件引用**:
```python
# 删除：
from models.hybrid_transformer import HybridSensorTransformer

# 删除所有Hybrid相关的：
- Tab界面
- 训练函数
- 配置参数
- 模型加载逻辑
```

**删除的Tab**:
- ❌ "Hybrid模型训练" tab
- ❌ 所有Hybrid相关UI组件
- ❌ temporal_signals选择器
- ❌ apply_smoothing选项

**简化的模型类型**:
```python
# 之前支持：
- StaticSensorTransformer
- HybridSensorTransformer

# 现在只支持：
- StaticSensorTransformer
```

### 🔄 修改内容

#### 1. 全局状态扩展
```python
global_state = {
    'df': None,
    'trained_models': {},
    'scalers': {},
    'residual_data': {},
    'residual_models': {},
    'residual_scalers': {},
    'final_predictions': {},
    'training_logs': {},
    'model_configs': {},
    'all_signals': [],
    # 新增：
    'stage2_models': {},      # Stage2残差模型
    'stage2_scalers': {},     # Stage2 Scalers
    'ensemble_models': {},    # 综合推理模型
    'sundial_models': {},     # Sundial时序预测模型
}
```

#### 2. 模型保存结构优化
```
saved_models/
├── StaticSensorTransformer_*.pth          # SST模型（保持不变）
├── StaticSensorTransformer_*.json         # SST推理配置（保持不变）
├── stage2_boost/                          # 新增：Stage2模型目录
│   ├── Stage2_Boost_*.pth
│   └── Stage2_Boost_*_scalers.pkl
└── ensemble/                              # 新增：综合模型目录
    └── Ensemble_*_config.json
```

#### 3. 残差提取功能保持
**保持原有功能**:
- ✅ 从SST模型提取残差
- ✅ 支持数据片段选择
- ✅ 残差可视化
- ✅ 残差数据保存

**删除**:
- ❌ Hybrid模型的残差提取逻辑

### 📊 性能对比展示

#### 原版 vs 增强版

| 功能 | 原版 | 增强版 |
|------|------|--------|
| SST训练 | ✅ | ✅ |
| Hybrid训练 | ✅ | ❌ 删除 |
| 残差提取 | ✅ | ✅ 增强 |
| TFT训练 | ✅ | ✅ 保留 |
| Stage2训练 | ❌ | ✅ **新增** |
| R²智能选择 | ❌ | ✅ **新增** |
| 综合推理模型 | ❌ | ✅ **新增** |
| 二次推理比较 | ❌ | ✅ **新增** |
| Sundial预测 | ❌ | ✅ **新增** |

### 🎯 核心算法说明

#### Stage2 Boost算法流程

```python
# 阶段1: SST模型训练
X_boundary → [SST Model] → Y_pred_base
Y_true - Y_pred_base = Residual_1

# 阶段2: Stage2残差模型训练
X_boundary → [Stage2 Model] → Residual_pred

# 阶段3: 智能R²选择
for each signal i:
    r2_i = R²(Y_true[:, i], Y_pred_base[:, i])
    if r2_i < threshold:
        Y_final[:, i] = Y_pred_base[:, i] + Residual_pred[:, i]
    else:
        Y_final[:, i] = Y_pred_base[:, i]

# 阶段4: 最终残差分析
Final_Residual = Y_true - Y_final

# 阶段5: Sundial时序预测（可选）
Final_Residual → [Sundial Model] → Future_Residual
```

#### R²阈值智能选择机制

```python
def intelligent_threshold_selection(y_true, y_pred_base, y_pred_stage2, threshold=0.4):
    """
    智能R²阈值选择
    
    原理：
    - 对于高R²信号（≥threshold）：SST已经预测得很好，不需要Stage2
    - 对于低R²信号（<threshold）：SST预测不够好，应用Stage2改进
    
    优势：
    1. 减少过拟合风险：高R²信号不应用Stage2
    2. 针对性改进：只对需要的信号应用Stage2
    3. 计算效率：减少不必要的Stage2计算
    """
    num_signals = y_true.shape[1]
    signal_decisions = []
    
    for i in range(num_signals):
        r2_base = r2_score(y_true[:, i], y_pred_base[:, i])
        
        if r2_base < threshold:
            # 低R²：应用Stage2
            apply_stage2 = True
            reason = f"R²={r2_base:.4f} < {threshold}"
        else:
            # 高R²：不应用Stage2
            apply_stage2 = False
            reason = f"R²={r2_base:.4f} ≥ {threshold}"
        
        signal_decisions.append({
            'signal_index': i,
            'r2_base': r2_base,
            'apply_stage2': apply_stage2,
            'reason': reason
        })
    
    return signal_decisions
```

### 🔧 技术细节

#### 1. Stage2模型架构
- **模型类型**: StaticSensorTransformer（与SST相同）
- **输入**: 边界传感器数据
- **输出**: 残差预测
- **训练目标**: 最小化残差预测误差

#### 2. 综合模型存储
```python
ensemble_info = {
    'name': 模型名称,
    'base_model_name': 基础SST模型名称,
    'stage2_model_name': Stage2模型名称,
    'r2_threshold': R²阈值,
    'signal_r2_scores': [每个信号的R²及决策],
    'metrics': {
        'base': {mae, rmse, r2},
        'ensemble': {mae, rmse, r2},
        'improvement': {mae_pct, rmse_pct, r2_pct}
    },
    'predictions': {
        'y_true': 真实值,
        'y_pred_base': SST预测,
        'y_pred_ensemble': 综合预测,
        'y_residual_pred': Stage2残差预测
    }
}
```

#### 3. 可视化改进
- **残差分析**: 4子图布局
- **训练历史**: 独立曲线
- **性能对比**: 表格+图表组合
- **二次推理**: 4子图对比（预测、误差、分布、指标）

### 📝 使用建议

#### 推荐工作流程

1. **数据准备** (10-20分钟)
   - 上传CSV数据
   - 检查数据质量
   - 选择信号

2. **SST训练** (30-60分钟)
   - 配置模型参数
   - 训练至收敛
   - 保存模型

3. **残差提取** (5-10分钟)
   - 选择SST模型
   - 提取全部或片段残差
   - 检查残差分布

4. **Stage2训练** (30-60分钟)
   - 选择残差数据
   - 训练Stage2模型
   - 评估残差改进

5. **生成综合模型** (2-5分钟)
   - 选择SST和Stage2模型
   - 设置R²阈值（建议0.4）
   - 查看信号级别决策

6. **二次推理验证** (5-10分钟)
   - 选择不同index范围
   - 对比性能提升
   - 关注特殊case

7. **Sundial预测** (可选, 20-30分钟)
   - 配置预测参数
   - 训练时序模型
   - 预测未来残差

#### R²阈值选择指南

| 阈值 | 应用场景 | 优势 | 劣势 |
|------|---------|------|------|
| 0.3 | 数据噪声大，需要强力修正 | Stage2应用更广 | 可能过拟合 |
| 0.4 | **推荐**，平衡性能和稳定性 | 平衡改进和稳定性 | - |
| 0.5 | 数据质量好，保守修正 | 避免过拟合 | 改进有限 |
| 0.6+ | 只修正极差信号 | 最稳定 | 改进最小 |

### 🐛 已知问题

1. **Sundial功能**
   - ⚠️ 当前为框架版本
   - ⏳ 完整实现待v2.0

2. **大数据集处理**
   - 💡 建议使用数据片段
   - 📊 单次推理建议<100K样本

3. **GPU显存**
   - 🎯 大模型需注意显存
   - 🔧 可减小batch_size

### 🔄 迁移指南

#### 从旧版本迁移

1. **备份旧模型**
```bash
cp -r saved_models saved_models_backup
```

2. **使用新版应用**
```bash
python gradio_residual_tft_app.py
```

3. **加载旧模型**
- 旧的SST模型可以直接使用
- Hybrid模型无法迁移（已删除支持）
- 需要重新训练Stage2模型

4. **推理配置兼容**
- 旧的SST推理配置完全兼容
- 可以直接上传使用

### 📈 预期性能提升

基于transformer_boost.ipynb的经验：

| 指标 | 基础SST | + Stage2 | 综合模型 | 改进幅度 |
|------|---------|----------|----------|---------|
| MAE | 1.000 | 0.850 | 0.800 | **15-25%** |
| RMSE | 1.000 | 0.880 | 0.850 | **12-20%** |
| R² | 0.850 | - | 0.920 | **显著** |

**注意**: 实际提升取决于数据特性和模型配置

### 💡 最佳实践

1. **分阶段训练**
   - SST → 残差提取 → Stage2 → 综合模型
   - 每个阶段验证后再进行下一步

2. **参数调优**
   - SST和Stage2使用相似的架构参数
   - Stage2可以适当降低复杂度

3. **R²阈值实验**
   - 从0.4开始
   - 观察性能变化
   - 根据需求调整

4. **数据片段策略**
   - 训练用全量数据
   - 验证用代表性片段
   - 二次推理用多个片段

### 🎓 技术参考

本实现参考了以下概念：

1. **Gradient Boosting**: 逐步修正残差的思想
2. **Ensemble Learning**: 组合多个模型的预测
3. **Selective Application**: 智能选择需要增强的部分
4. **Time Series Analysis**: Sundial时序预测

### 📞 支持与反馈

如有问题或建议：
1. 检查本文档的已知问题部分
2. 查看README.md的详细说明
3. 提交Issue描述问题

---

**更新日期**: 2025-10-22
**版本**: v1.0 Enhanced
**维护者**: Industrial Digital Twin Team
