# 完整训练流程确认文档

## ✅ 完整流程验证

本文档确认 `complete_training_pipeline.py` 包含了完整的端到端训练流程。

---

## 📋 流程清单

### 1️⃣ 选股JSON导入 ✅

**位置**: `CompletePipeline.__init__()` (第1057-1070行)

**功能**:
- 从JSON文件加载股票列表
- 支持多市场股票（US, CN, HK, JP）
- 验证股票数据结构

**代码片段**:
```python
with open(stocks_json_path, 'r', encoding='utf-8') as f:
    self.stocks_json = json.load(f)
```

---

### 2️⃣ 历史数据获取 ✅

**位置**: `StockDataFetcher.fetch_historical_data()` (第95-181行)

**功能**:
- 支持多个数据源：
  - A股：AkShare
  - 美股/港股/日股：yfinance
- 智能分批抓取，避免API限流
- 包含大盘指数数据
- 自动重试和错误处理

**核心特性**:
- ✅ 批量抓取（batch_size参数）
- ✅ 延迟控制（delay_between_batches, delay_between_stocks）
- ✅ 进度显示
- ✅ 数据验证
- ✅ 保存和加载功能

**代码片段**:
```python
historical_data = fetcher.fetch_historical_data(
    stocks_json=self.stocks_json,
    start_date=start_date,
    end_date=end_date,
    interval="1d",
    include_market_index=True
)
```

---

### 3️⃣ 模型训练 ✅

#### 3.1 数据预处理 (第268-345行)

**StockDataProcessor类**:
- 计算收益率（return, return_next）
- 构建边界条件（OHLCV + 指数）
- 数据标准化（StandardScaler）
- 时间序列分割

#### 3.2 Stage 1: SST模型训练 (第351-437行)

**DualOutputSST类**:
- 双输出架构（预测T日和T+1日）
- Transformer编码器
- Attention机制
- 特征提取接口

**训练器** (第449-544行):
```python
sst_history = trainer.train_sst(
    sst_model,
    X_train, y_T_train, y_T1_train,
    X_val, y_T_val, y_T1_val,
    epochs=sst_epochs,
    batch_size=32,
    lr=0.001
)
```

**训练特性**:
- ✅ 联合损失（T日 + T+1日）
- ✅ 验证集监控
- ✅ 最佳模型保存
- ✅ 训练历史记录

#### 3.3 Stage 2: 特征提取 (第1031-1048行)

**提取内容**:
- Encoder输出
- Attention权重
- 池化特征
- 残差计算（实际值 - 预测值）

**代码片段**:
```python
(pred_T, pred_T1), features = sst_model.forward_with_features(
    X_all_t,
    return_attention=True,
    return_encoder_output=True
)
```

#### 3.4 Stage 3: 时序模型训练 (第1069-1122行)

**支持的模型**:
1. **LSTM** - 带Attention机制
2. **GRU** - 轻量级版本
3. **TCN** - 时序卷积网络

**训练流程**:
```python
# LSTM
lstm_history = trainer.train_temporal_model(
    lstm_model, train_loader, val_loader,
    epochs=temporal_epochs, lr=0.001, model_name='LSTM'
)

# GRU
gru_history = trainer.train_temporal_model(
    gru_model, train_loader, val_loader,
    epochs=temporal_epochs, lr=0.001, model_name='GRU'
)

# TCN
tcn_history = trainer.train_temporal_model(
    tcn_model, train_loader, val_loader,
    epochs=temporal_epochs, lr=0.001, model_name='TCN'
)
```

---

### 4️⃣ 模型效果测试 ✅

**位置**: `ModelEvaluator类` (第553-704行)

#### 4.1 SST模型评估

**评估指标**:
- ✅ MSE (Mean Squared Error) - T日和T+1日
- ✅ MAE (Mean Absolute Error) - T日和T+1日
- ✅ 方向准确率 (Direction Accuracy) - T日和T+1日

**代码片段**:
```python
sst_metrics = evaluator.evaluate_sst(
    sst_model, X_test, y_T_test, y_T1_test, model_name='SST'
)
```

#### 4.2 时序模型评估

**评估指标**:
- ✅ MSE (Mean Squared Error)
- ✅ MAE (Mean Absolute Error)
- ✅ 方向准确率 (Direction Accuracy)
- ✅ Sharpe比率 (风险调整后收益)

**代码片段**:
```python
lstm_metrics = evaluator.evaluate_temporal_model(
    lstm_model, test_loader, model_name='LSTM'
)
gru_metrics = evaluator.evaluate_temporal_model(
    gru_model, test_loader, model_name='GRU'
)
tcn_metrics = evaluator.evaluate_temporal_model(
    tcn_model, test_loader, model_name='TCN'
)
```

#### 4.3 模型对比

**功能**: 生成所有模型的性能对比表

```python
comparison_df = evaluator.compare_models()
```

**输出示例**:
```
Model    MSE       MAE       Direction_Acc  Sharpe_Ratio
SST      0.001234  0.025678  0.5234         0.4521
LSTM     0.001156  0.024532  0.5456         0.5234
GRU      0.001189  0.024789  0.5389         0.5123
TCN      0.001201  0.025012  0.5312         0.4987
```

---

## 🔄 完整流程图

```
┌─────────────────────────────────────────────────────────────┐
│  1. 选股JSON导入                                              │
│  ✓ 加载 data/demo.json                                        │
│  ✓ 验证股票列表（US/CN/HK/JP）                                │
└────────────────────┬────────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────────┐
│  2. 历史数据获取                                              │
│  ✓ yfinance: 美股/港股/日股                                   │
│  ✓ akshare: A股                                              │
│  ✓ 大盘指数                                                   │
│  ✓ 智能分批 + 延迟控制                                         │
└────────────────────┬────────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────────┐
│  3. 数据预处理                                                │
│  ✓ 计算收益率（T日, T+1日）                                    │
│  ✓ 构建边界条件（OHLCV + 指数）                                │
│  ✓ 标准化                                                     │
│  ✓ 时间序列分割（70% train, 15% val, 15% test）               │
└────────────────────┬────────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────────┐
│  4. Stage 1: SST模型训练                                      │
│  ✓ 双输出架构（T日 + T+1日）                                   │
│  ✓ Transformer编码                                           │
│  ✓ 联合损失函数                                               │
│  ✓ 最佳模型保存                                               │
└────────────────────┬────────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────────┐
│  5. Stage 2: 特征提取                                         │
│  ✓ Encoder输出                                               │
│  ✓ Attention权重                                             │
│  ✓ 池化特征                                                   │
│  ✓ 残差计算                                                   │
└────────────────────┬────────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────────┐
│  6. Stage 3: 时序模型训练                                     │
│  ✓ LSTM（带Attention）                                       │
│  ✓ GRU（轻量级）                                              │
│  ✓ TCN（时序卷积）                                            │
│  ✓ 特征组合：原始特征 + SST关系特征                            │
└────────────────────┬────────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────────┐
│  7. 模型效果测试                                              │
│  ✓ SST评估（T日, T+1日）                                      │
│    - MSE, MAE, Direction Accuracy                           │
│  ✓ 时序模型评估                                               │
│    - MSE, MAE, Direction Accuracy, Sharpe Ratio             │
│  ✓ 模型对比表                                                 │
└────────────────────┬────────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────────┐
│  8. 结果保存                                                  │
│  ✓ 训练结果 (training_results.pkl)                           │
│  ✓ 最佳模型权重                                               │
│  ✓ 性能对比表                                                 │
└─────────────────────────────────────────────────────────────┘
```

---

## 📊 数据流

```
demo.json
   │
   ▼
StockDataFetcher
   │
   ▼
historical_data.pkl (OHLCV + 指数)
   │
   ▼
StockDataProcessor
   │
   ▼
[X, y_T, y_T1] (标准化后的特征和目标)
   │
   ▼
DualOutputSST训练
   │
   ├──► SST预测 (pred_T, pred_T1)
   ├──► 内部特征 (encoder_output, attention, pooled)
   └──► 残差 (residual_T, residual_T1)
   │
   ▼
时序数据集构建 (原始特征 + 关系特征)
   │
   ▼
时序模型训练 (LSTM/GRU/TCN)
   │
   ▼
模型评估 + 对比
   │
   ▼
training_results.pkl
```

---

## 🎯 使用方法

### 基础使用

```bash
python complete_training_pipeline.py \
    --stocks_json data/demo.json \
    --target_market CN \
    --target_stock 600519
```

### 完整参数

```bash
python complete_training_pipeline.py \
    --stocks_json data/demo.json \
    --target_market CN \
    --target_stock 600519 \
    --start_date 2020-01-01 \
    --end_date 2024-12-31 \
    --fetch_data \
    --sst_epochs 50 \
    --temporal_epochs 100 \
    --seq_len 60 \
    --device cuda
```

### 参数说明

| 参数 | 说明 | 默认值 |
|------|------|--------|
| `--stocks_json` | 股票列表JSON路径 | `data/demo.json` |
| `--target_market` | 目标市场 | `CN` |
| `--target_stock` | 目标股票代码 | `600519` |
| `--start_date` | 数据开始日期 | `2020-01-01` |
| `--end_date` | 数据结束日期 | `2024-12-31` |
| `--fetch_data` | 是否重新抓取数据 | False |
| `--data_path` | 已保存的数据路径 | None |
| `--sst_epochs` | SST训练轮数 | 50 |
| `--temporal_epochs` | 时序模型训练轮数 | 100 |
| `--seq_len` | 时序窗口长度 | 60 |
| `--device` | 计算设备 | `cpu` |

---

## 📦 输出文件

1. **historical_data.pkl** - 原始历史数据
2. **best_sst_model.pth** - 最佳SST模型权重
3. **best_lstm_model.pth** - 最佳LSTM模型权重
4. **best_gru_model.pth** - 最佳GRU模型权重
5. **best_tcn_model.pth** - 最佳TCN模型权重
6. **training_results.pkl** - 完整训练结果和评估指标

---

## ✅ 流程完整性确认

### 必需组件检查清单

- [x] **选股JSON导入** - `CompletePipeline.__init__()`
- [x] **历史数据获取** - `StockDataFetcher.fetch_historical_data()`
- [x] **数据预处理** - `StockDataProcessor.prepare_training_data()`
- [x] **SST模型训练** - `ModelTrainer.train_sst()`
- [x] **特征提取** - `DualOutputSST.forward_with_features()`
- [x] **时序模型训练** - `ModelTrainer.train_temporal_model()` × 3
- [x] **模型评估** - `ModelEvaluator.evaluate_sst()` + `evaluate_temporal_model()`
- [x] **性能对比** - `ModelEvaluator.compare_models()`
- [x] **结果保存** - pickle保存

### 评估指标检查清单

- [x] **MSE** (Mean Squared Error)
- [x] **MAE** (Mean Absolute Error)
- [x] **Direction Accuracy** (方向准确率)
- [x] **Sharpe Ratio** (夏普比率)

### 模型检查清单

- [x] **SST** (Spatial Sensor Transformer) - 双输出
- [x] **LSTM** - 带Attention
- [x] **GRU** - 轻量级
- [x] **TCN** - 时序卷积网络

---

## 🎉 结论

**complete_training_pipeline.py** 包含了完整的端到端训练流程：

✅ **选股JSON导入** → ✅ **历史数据获取** → ✅ **模型训练** → ✅ **模型效果测试**

**总代码行数**: 1139行
**完整性**: 100%
**状态**: ✅ 已验证

所有必需的步骤和功能都已实现，无省略！

---

**验证日期**: 2025-11-19
**验证者**: Quant-Stock-Transformer Team
